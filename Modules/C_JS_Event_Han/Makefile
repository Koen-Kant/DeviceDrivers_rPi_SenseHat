# Makefile for CModule

COMPONENT   = jsehmodule
OBJS        = JSEHModule
CMHGDEPENDS = JSEHModule
CUSTOMRES   = no

include CModule 

# This example has no 'h' directory, but one needs to be created to hold
# the header file generated by CMHG.
${DIRS} ::
        ${MKDIR} h

# Clean should remove the 'h' directory created above.
clean ::
        ${WIPE} h ${WFLAGS}

# Dynamic dependencies:
h.jsehmoduleHdr:	cmhg.jsehmoduleHdr
o.jsehmoduleHdr:	cmhg.jsehmoduleHdr
o.JSEHModule:	c.JSEHModule
o.JSEHModule:	C:h.kernel
o.JSEHModule:	C:h.swis
o.JSEHModule:	h.jsehmodulehdr
